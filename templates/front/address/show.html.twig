{% extends 'base.html.twig' %}

{% block title %}Address{% endblock %}

{% block body %}
    <h1>Address</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ address.id }}</td>
            </tr>
            <tr>
                <th>Street</th>
                <td>{{ address.street }}</td>
            </tr>
            <tr>
                <th>Zc</th>
                <td>{{ address.zc }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ address.city }}</td>
            </tr>
            <tr>
                <th>Country</th>
                <td>{{ address.country }}</td>
            </tr>
            <tr>
                <th>IsDefault</th>
                <td>{{ address.isDefault }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('front_address_index') }}">back to list</a>

    <a href="{{ path('front_address_edit', {'id': address.id}) }}">edit</a>

    {{ include('front/address/_delete_form.html.twig') }}
{% endblock %}
