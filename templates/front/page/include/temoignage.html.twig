<section>
    <div class="container">
        <div class="section-title">
            <h2>Témoignages</h2>
            <div class="section-subtitle">Témoignages</div>
            <span class="section-separator"></span>
        </div>
    </div>
    <div class="carousel fl-wrap">
        <div class="testimonials-carousel single-carousel fl-wrap">
            {% for commentaire in commentaires  %}
                <div class="slick-slide-item">
                    <div class="testimonilas-text">
                        <div class="listing-rating card-popup-rainingvis" data-starrating2="{{ commentaire.rating }}"></div>
                        <p>{{commentaire.comment}}</p>
                    </div>
                    <div class="testimonilas-avatar-item">
                        <div class="testimonilas-avatar"><img src="{{ asset(commentaire.giver.avatar) }}" alt=""></div>
                        <h4>{{ commentaire.giver.firstname }} {{ commentaire.giver.lastName }}</h4>
                        <span>Voyageur</span>
                    </div>
                </div>
            {% endfor %}

        </div>
        <div class="swiper-button-prev sw-btn">
            <i class="fa fa-angle-left"></i>
        </div>
        <div class="swiper-button-next sw-btn">
            <i class="fa fa-angle-right"></i>
        </div>
    </div>
</section>