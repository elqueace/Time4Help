{% extends 'base.html.twig' %}

{% block title %}Booking{% endblock %}

{% block body %}
    <h1>Booking</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ booking.id }}</td>
            </tr>
            <tr>
                <th>IsPayed</th>
                <td>{{ booking.isPayed ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>IsAccepted</th>
                <td>{{ booking.isAccepted ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('front_booking_index') }}">back to list</a>

    <a href="{{ path('front_booking_edit', {'id': booking.id}) }}">edit</a>

    {{ include('front/booking/_delete_form.html.twig') }}
{% endblock %}
